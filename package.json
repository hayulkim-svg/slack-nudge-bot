{
  "name": "slack-nudge-bot",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "engines": { "node": ">=26" },
  "scripts": {
    "test": "node --test",
    "announce": "node --env-file=.env src/announce.js",
    "check": "node --env-file=.env src/check.js"
  },
  "dependencies": {
    "@slack/web-api": "^7.0.0"
  }
}
